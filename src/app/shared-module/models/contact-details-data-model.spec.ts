import { ContactDetailsDataModel } from './contact-details-data-model';

describe('ContactDetailsDataModel', () => {
  it('should create an instance', () => {
    expect(new ContactDetailsDataModel()).toBeTruthy();
  });
});
