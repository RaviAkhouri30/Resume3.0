import { EducationDataModel } from './education-data-model';

describe('EducationDataModel', () => {
  it('should create an instance', () => {
    expect(new EducationDataModel()).toBeTruthy();
  });
});
