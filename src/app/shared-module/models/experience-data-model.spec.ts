import { ExperienceDataModel } from './experience-data-model';

describe('ExperienceDataModel', () => {
  it('should create an instance', () => {
    expect(new ExperienceDataModel()).toBeTruthy();
  });
});
