import { ProjectsExperienceDataModel } from './projects-experience-data-model.js';

describe('ProjectsExperienceDataModelTs', () => {
  it('should create an instance', () => {
    expect(new ProjectsExperienceDataModel()).toBeTruthy();
  });
});
