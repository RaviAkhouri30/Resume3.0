import { HandleErrorFactory } from './handle-error-factory';

describe('HanldeErrorFactory', () => {
  it('should create an instance', () => {
    expect(new HandleErrorFactory()).toBeTruthy();
  });
});
