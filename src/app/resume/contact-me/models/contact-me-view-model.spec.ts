import { ContactMeViewModel } from './contact-me-view-model';

describe('ContactMeViewModel', () => {
  it('should create an instance', () => {
    expect(new ContactMeViewModel()).toBeTruthy();
  });
});
