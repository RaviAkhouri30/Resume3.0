import { ProjectsExperienceModel } from './projects-experience-model';

describe('ProjectsExperienceModel', () => {
  it('should create an instance', () => {
    expect(new ProjectsExperienceModel()).toBeTruthy();
  });
});
